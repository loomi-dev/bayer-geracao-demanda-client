import { defineStyleConfig } from '@chakra-ui/react';

export const Tooltip = defineStyleConfig({
  baseStyle: {
    layerStyle: 'card',
    color: 'text.primary',
    fontSize: '1.4rem',
    borderRadius: '1.6rem',
    py: '1rem',
    px: '1.6rem',
  },
});
