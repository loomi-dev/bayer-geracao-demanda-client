import { BalanceSection, PlanningTable, StatisticsSection } from '../components';

export const PlanningScreen = () => (
  <>
    <StatisticsSection />
    <BalanceSection />
    <PlanningTable />
  </>
);
